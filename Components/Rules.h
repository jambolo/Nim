#pragma once

// Various rules for the game

namespace Rules
{

enum class Variation
{
    MISERE = 0,         // Misère
    NORMAL,             // Normal
    DEFAULT = MISERE    // Default variation
};

} // namespace Rules
